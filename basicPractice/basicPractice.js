// Variables and Data Types:

// Declare variables of different data types (string, number, boolean).
// let name = "andrew";
// let number = 1;
// let number5 = 5;
// let country = "UK"
// let happy = true;

// console.log(name, number, happy);



// Perform operations (arithmetic, comparison) on variables.
// console.log(number * number5);

// Concatenate strings and convert between data types.
// console.log(country + name)

// Operators:

// Create expressions using arithmetic operators (+, -, *, /, %).
// Use comparison operators (==, ===, !=, !==, <, >, <=, >=) in conditional statements.
// function dataCheck(number) {
//     if (number === 1) {
//         console.log("correct");
//     } 
//     else if (number === 3) {
//         console.log("ok that's allowed");
//     }
//     else {
//         console.log("INCORRECT");
//     }
// }








// dataCheck(3);

// Experiment with logical operators (&&, ||, !) to form complex conditions.
// Control Flow:

let x = 5;
let y = 10;

function weatherMan(arg) {
if (arg > 0 && arg < 15) {
console.log("both conditions are true");
}
else { console.log("somethings wrong");
}
}

weatherMan(13)

// let dog = "happy"
// let cat = "sad"

// let animalCentre(arg)
// if (dog)






// Write if-else statements to handle different conditions.
// Implement switch-case statements for multiple conditional branches.
// Practice using loops (for, while) to iterate over arrays or perform repetitive tasks.
// Functions:

// Define functions with and without parameters.
// Return values from functions and store them in variables.
// Create higher-order functions that accept other functions as arguments.
// Arrays:

// Declare and initialize arrays with different types of elements.
// Access elements by index and modify array contents.
// Use array methods like push(), pop(), shift(), unshift(), slice(), splice().
// Objectives:

// Create objects with key-value pairs representing properties and values.
// Access object properties using dot notation and bracket notation.
// Practice with methods and prototype inheritance.
// Mini Projects:

// Implement a simple calculator that performs basic arithmetic operations.
// Write a program that checks if a given year is a leap year.
// Create a function that reverses a string without using built-in methods.
// Develop a small application that generates a random password based on user preferences.